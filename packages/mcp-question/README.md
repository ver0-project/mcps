# MCP Question Server

An MCP (Model Context Protocol) server that provides

## Installation

```json
{
  "mcpServers": {
    "git": {
      "command": "npx",
      "args": ["-y", "@ver0/mcp-question"]
    }
  }
}
```
